spring:
  datasource:
    druid:
      driver-class-name: com.mysql.jdbc.Driver
      username: root
      password: root
      url: jdbc:mysql://localhost:3306/sport_mall?characterEncoding=utf-8

mybatis:
  mapper-locations: classpath:mappers/*Mapper.xml
  type-aliases-package: com.cdu.edu.sportmall.entity